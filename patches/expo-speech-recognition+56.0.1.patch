diff --git a/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoAudioRecorder.kt b/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoAudioRecorder.kt
index aaa462a..4f1e45c 100644
--- a/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoAudioRecorder.kt
+++ b/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoAudioRecorder.kt
@@ -10,11 +10,12 @@ import android.os.ParcelFileDescriptor.AutoCloseOutputStream
 import android.os.Build
 import android.util.Log
 import androidx.annotation.RequiresApi
+import java.io.BufferedOutputStream
 import java.io.DataOutputStream
 import java.io.File
 import java.io.FileOutputStream
 import java.io.IOException
-import java.util.UUID
+import java.io.RandomAccessFile
 import kotlin.concurrent.thread
 
 interface AudioRecorder {
@@ -39,13 +40,13 @@ class ExpoAudioRecorder(
     var outputFile: File? = null
     var outputFileUri = "file://$outputFilePath"
 
-    /** The file where the mic stream is being output to */
-    private val tempPcmFile: File
+    /** A valid, incrementally checkpointed WAV file; no stop-time copy is required. */
+    private val wavFile: File?
     val recordingParcel: ParcelFileDescriptor
     private var outputStream: AutoCloseOutputStream?
 
     init {
-        tempPcmFile = createTempPcmFile()
+        wavFile = outputFilePath?.let(::prepareWavFile)
         try {
             val pipe = ParcelFileDescriptor.createPipe()
             recordingParcel = pipe[0]
@@ -73,62 +74,64 @@ class ExpoAudioRecorder(
                 .reverseBytes(s)
                 .toInt()
 
-        fun appendWavHeader(
-            outputFilePath: String,
-            pcmFile: File,
-            sampleRateInHz: Int,
-        ): File {
-            val outputFile = File(outputFilePath)
-            val audioDataLength = pcmFile.length()
-            val numChannels = 1
-            val bitsPerSample = 16
-
-            DataOutputStream(FileOutputStream(outputFile)).use { out ->
-                val totalDataLen = 36 + audioDataLength
-                val byteRate = sampleRateInHz * numChannels * bitsPerSample / 8
-                val blockAlign = numChannels * bitsPerSample / 8
-
-                // Write the RIFF chunk descriptor
-                out.writeBytes("RIFF") // ChunkID
-                out.writeInt(Integer.reverseBytes(totalDataLen.toInt())) // ChunkSize
-                out.writeBytes("WAVE")
-                out.writeBytes("fmt ")
-                out.writeInt(Integer.reverseBytes(16)) // Subchunk1Size (16 for PCM)
-                out.writeShort(shortReverseBytes(1)) // AudioFormat (1 for PCM)
-                out.writeShort(shortReverseBytes(numChannels.toShort())) // NumChannels
-                out.writeInt(Integer.reverseBytes(sampleRateInHz)) // SampleRate
-                out.writeInt(Integer.reverseBytes(byteRate)) // ByteRate
-                out.writeShort(shortReverseBytes(blockAlign.toShort())) // BlockAlign
-                out.writeShort(shortReverseBytes(bitsPerSample.toShort())) // BitsPerSample
-
-                // Write the data sub-chunk
-                out.writeBytes("data")
-                out.writeInt(Integer.reverseBytes(audioDataLength.toInt()))
+        private fun initialiseWav(file: File) {
+            DataOutputStream(FileOutputStream(file, false)).use { out ->
+                writeHeader(out, 0L, 16000)
+            }
+        }
 
-                try {
-                    val pcmData = pcmFile.readBytes()
-                    out.write(pcmData)
-                    pcmFile.delete()
-                } catch (e: IOException) {
-                    Log.e(TAG, "Failed to read PCM file", e)
-                    e.printStackTrace()
+        private fun prepareWavFile(path: String): File? =
+            runCatching {
+                File(path).apply {
+                    parentFile?.mkdirs()
+                    if (!exists()) createNewFile()
+                    initialiseWav(this)
                 }
+            }.getOrElse { error ->
+                Log.e(TAG, "Failed to prepare WAV file at $path", error)
+                null
             }
 
-            return outputFile
+        private fun writeHeader(out: DataOutputStream, audioDataLength: Long, sampleRateInHz: Int) {
+            val numChannels = 1
+            val bitsPerSample = 16
+            val totalDataLen = 36L + audioDataLength
+            require(totalDataLen <= 0xffffffffL) { "WAV file exceeds the RIFF size limit" }
+            val byteRate = sampleRateInHz * numChannels * bitsPerSample / 8
+            val blockAlign = numChannels * bitsPerSample / 8
+            out.writeBytes("RIFF")
+            out.writeInt(Integer.reverseBytes(totalDataLen.toInt()))
+            out.writeBytes("WAVE")
+            out.writeBytes("fmt ")
+            out.writeInt(Integer.reverseBytes(16))
+            out.writeShort(shortReverseBytes(1))
+            out.writeShort(shortReverseBytes(numChannels.toShort()))
+            out.writeInt(Integer.reverseBytes(sampleRateInHz))
+            out.writeInt(Integer.reverseBytes(byteRate))
+            out.writeShort(shortReverseBytes(blockAlign.toShort()))
+            out.writeShort(shortReverseBytes(bitsPerSample.toShort()))
+            out.writeBytes("data")
+            out.writeInt(Integer.reverseBytes(audioDataLength.toInt()))
         }
-    }
 
-    private fun createTempPcmFile(): File {
-        val file = File(context.cacheDir, "temp_${UUID.randomUUID()}.pcm")
-        if (!file.exists()) {
-            try {
-                file.createNewFile()
-            } catch (e: IOException) {
-                e.printStackTrace()
+        private fun checkpointHeader(file: File) {
+            if (!file.exists()) {
+                Log.w(TAG, "Skipping WAV checkpoint because the file is missing: ${file.absolutePath}")
+                return
+            }
+            val audioDataLength = (file.length() - 44L).coerceAtLeast(0L)
+            if (audioDataLength > 0xffffffffL - 36L) return
+            runCatching {
+                RandomAccessFile(file, "rw").use { wav ->
+                    wav.seek(4L)
+                    wav.writeInt(Integer.reverseBytes((36L + audioDataLength).toInt()))
+                    wav.seek(40L)
+                    wav.writeInt(Integer.reverseBytes(audioDataLength.toInt()))
+                }
+            }.onFailure { error ->
+                Log.e(TAG, "Failed to checkpoint WAV header for ${file.absolutePath}", error)
             }
         }
-        return file
     }
 
     @SuppressLint("MissingPermission")
@@ -163,22 +166,29 @@ class ExpoAudioRecorder(
 
     override fun stop() {
         isRecordingAudio = false
-        audioRecorder?.stop()
+        try {
+            audioRecorder?.stop()
+        } catch (_: IllegalStateException) {}
+        // A pipe write can remain blocked after AudioRecord stops. Closing the
+        // writer first guarantees the capture thread can reach its WAV flush.
+        try {
+            outputStream?.close()
+            outputStream = null
+        } catch (_: IOException) {}
+        val thread = recordingThread
+        if (thread != null && thread !== Thread.currentThread()) {
+            try {
+                thread.join(5000)
+            } catch (_: InterruptedException) {
+                Thread.currentThread().interrupt()
+            }
+        }
         audioRecorder?.release()
         audioRecorder = null
         recordingThread = null
-        if (outputFilePath != null) {
-            try {
-                outputFile =
-                    appendWavHeader(
-                        outputFilePath,
-                        tempPcmFile,
-                        sampleRateInHz,
-                    )
-            } catch (e: IOException) {
-                Log.e(TAG, "Failed to append WAV header", e)
-                e.printStackTrace()
-            }
+        wavFile?.let {
+            checkpointHeader(it)
+            outputFile = it.takeIf(File::exists)
         }
         // Close the ParcelFileDescriptor
         try {
@@ -186,74 +196,88 @@ class ExpoAudioRecorder(
         } catch (e: IOException) {
             e.printStackTrace()
         }
-        // And the output stream
-        try {
-            outputStream?.close()
-            outputStream = null
-        } catch (e: IOException) {
-            e.printStackTrace()
-        }
     }
 
     private fun streamAudioToPipe() {
-        val tempFileOutputStream = FileOutputStream(tempPcmFile)
+        val wavOutputStream = wavFile?.let { file ->
+            runCatching {
+                BufferedOutputStream(FileOutputStream(file, true), 64 * 1024)
+            }.getOrElse { error ->
+                Log.e(TAG, "Failed to open WAV output stream for ${file.absolutePath}", error)
+                null
+            }
+        }
         val data = ByteArray(bufferSizeInBytes)
+        var bytesSinceCheckpoint = 0L
+        // A process kill loses at most five seconds from the WAV header length.
+        val checkpointBytes = sampleRateInHz * 2L * 5L
 
-        while (isRecordingAudio) {
-            val recorder = audioRecorder ?: break
-
-            val read =
-                if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
-                    // avoid empty reads
-                    recorder.read(data, 0, data.size, AudioRecord.READ_BLOCKING)
-                } else {
-                    recorder.read(data, 0, data.size)
-                }
-
-            when {
-                read > 0 -> {
-                    try {
-                        outputStream?.write(data, 0, read)
-                        outputStream?.flush()
+        try {
+            while (isRecordingAudio) {
+                val recorder = audioRecorder ?: break
+
+                val read =
+                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
+                        recorder.read(data, 0, data.size, AudioRecord.READ_BLOCKING)
+                    } else {
+                        recorder.read(data, 0, data.size)
+                    }
 
-                        // Write to the temp PCM file
-                        if (outputFilePath != null) {
-                            tempFileOutputStream.write(data, 0, read)
-                            tempFileOutputStream.flush()
+                when {
+                    read > 0 -> {
+                        try {
+                            outputStream?.write(data, 0, read)
+                            wavOutputStream?.write(data, 0, read)
+                            bytesSinceCheckpoint += read
+                            if (bytesSinceCheckpoint >= checkpointBytes) {
+                                wavOutputStream?.flush()
+                                wavFile?.let(::checkpointHeader)
+                                bytesSinceCheckpoint = 0L
+                            }
+                        } catch (e: IOException) {
+                            Log.e(TAG, "Failed to write microphone audio", e)
+                            break
                         }
-                    } catch (e: IOException) {
-                        Log.e(TAG, "Failed to write to output stream", e)
-                        e.printStackTrace()
                     }
-                }
 
-                // (this should only happen on API 22 and below)
-                read == 0 -> {
-                    try {
-                        Thread.sleep(10)
-                    } catch (_: InterruptedException) {}
-                }
+                    read == 0 -> {
+                        try {
+                            Thread.sleep(10)
+                        } catch (_: InterruptedException) {
+                            Thread.currentThread().interrupt()
+                            break
+                        }
+                    }
 
-                read == AudioRecord.ERROR_DEAD_OBJECT -> {
-                    Log.w(TAG, "AudioRecord returned ERROR_DEAD_OBJECT; breaking out of the loop")
-                    // todo: we should probably emit an error event here
-                    break
-                }
+                    read == AudioRecord.ERROR_DEAD_OBJECT -> {
+                        Log.w(TAG, "AudioRecord returned ERROR_DEAD_OBJECT; ending capture")
+                        break
+                    }
 
-                read == AudioRecord.ERROR_INVALID_OPERATION || read == AudioRecord.ERROR_BAD_VALUE -> {
-                    Log.w(TAG, "AudioRecord read error: $read; backing off briefly")
-                    try {
-                        Thread.sleep(10)
-                    } catch (_: InterruptedException) {}
-                }
+                    read == AudioRecord.ERROR_INVALID_OPERATION || read == AudioRecord.ERROR_BAD_VALUE -> {
+                        Log.w(TAG, "AudioRecord read error: $read; backing off briefly")
+                        try {
+                            Thread.sleep(10)
+                        } catch (_: InterruptedException) {
+                            Thread.currentThread().interrupt()
+                            break
+                        }
+                    }
 
-                else -> {
-                    // todo: we should probably emit an error event here
-                    Log.w(TAG, "AudioRecord read returned '$read'; breaking out of the loop")
-                    break
+                    else -> {
+                        Log.w(TAG, "AudioRecord read returned '$read'; ending capture")
+                        break
+                    }
                 }
             }
+        } finally {
+            try {
+                wavOutputStream?.flush()
+                wavOutputStream?.close()
+                wavFile?.let(::checkpointHeader)
+            } catch (e: IOException) {
+                Log.e(TAG, "Failed to finalize WAV checkpoint", e)
+            }
         }
-        tempFileOutputStream.close()
     }
 }
diff --git a/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoSpeechService.kt b/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoSpeechService.kt
index 06ff2e0..9b72da5 100644
--- a/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoSpeechService.kt
+++ b/node_modules/expo-speech-recognition/android/src/main/java/expo/modules/speechrecognition/ExpoSpeechService.kt
@@ -405,7 +405,10 @@ class ExpoSpeechService(
                 is String -> intent.putExtra(fieldValue, value)
                 is List<*> -> {
                     if (value.all { it is String }) {
-                        intent.putExtra(fieldValue, value.filterIsInstance<String>().toTypedArray())
+                        // RecognizerIntent's language allow-list extras are documented
+                        // as lists. Use the Android-native ArrayList representation;
+                        // some Pixel recognizers ignore a String[] for these extras.
+                        intent.putStringArrayListExtra(fieldValue, ArrayList(value.filterIsInstance<String>()))
                     }
                 }
                 is Double -> intent.putExtra(fieldValue, value.toInt())
@@ -631,6 +634,12 @@ class ExpoSpeechService(
     override fun onLanguageDetection(results: Bundle) {
         val detectedLanguage = results.getString(SpeechRecognizer.DETECTED_LANGUAGE)
         val confidence = languageDetectionConfidenceLevelToFloat(results.getInt(SpeechRecognizer.LANGUAGE_DETECTION_CONFIDENCE_LEVEL))
+        val switchResult =
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
+                results.getInt(SpeechRecognizer.LANGUAGE_SWITCH_RESULT, -1)
+            } else {
+                -1
+            }
 
         // Only send event if language or confidence has changed
         if (detectedLanguage != lastDetectedLanguage || confidence != lastLanguageConfidence) {
@@ -643,11 +652,26 @@ class ExpoSpeechService(
                     "detectedLanguage" to detectedLanguage,
                     "confidence" to confidence,
                     "topLocaleAlternatives" to results.getStringArrayList(SpeechRecognizer.TOP_LOCALE_ALTERNATIVES),
+                    "languageSwitchResultCode" to switchResult,
+                    "languageSwitchResult" to languageSwitchResultName(switchResult),
                 ),
             )
         }
     }
 
+    private fun languageSwitchResultName(value: Int): String =
+        if (Build.VERSION.SDK_INT < Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
+            "unavailable"
+        } else {
+            when (value) {
+                SpeechRecognizer.LANGUAGE_SWITCH_RESULT_SUCCEEDED -> "succeeded"
+                SpeechRecognizer.LANGUAGE_SWITCH_RESULT_FAILED -> "failed"
+                SpeechRecognizer.LANGUAGE_SWITCH_RESULT_SKIPPED_NO_MODEL -> "skipped-no-model-or-not-allowed"
+                SpeechRecognizer.LANGUAGE_SWITCH_RESULT_NOT_ATTEMPTED -> "not-attempted"
+                else -> "unknown"
+            }
+        }
+
     /**
      * For API 33: Basically same as onResults but doesn't stop
      */
diff --git a/node_modules/expo-speech-recognition/build/ExpoSpeechRecognitionModule.types.d.ts b/node_modules/expo-speech-recognition/build/ExpoSpeechRecognitionModule.types.d.ts
index 319bdcb..ff26045 100644
--- a/node_modules/expo-speech-recognition/build/ExpoSpeechRecognitionModule.types.d.ts
+++ b/node_modules/expo-speech-recognition/build/ExpoSpeechRecognitionModule.types.d.ts
@@ -106,6 +106,9 @@ export type LanguageDetectionEvent = {
     confidence: number;
     /** The alternative locales for the same language, in BCP-47 format. e.g. ["en-US", "en-GB"] */
     topLocaleAlternatives: string[];
+    /** Android 14+: whether the recognizer actually switched models. */
+    languageSwitchResult?: "succeeded" | "failed" | "skipped-no-model-or-not-allowed" | "not-attempted" | "unavailable" | "unknown";
+    languageSwitchResultCode?: number;
 };
 /**
  * Events that are dispatched from the native side
diff --git a/node_modules/expo-speech-recognition/src/ExpoSpeechRecognitionModule.types.ts b/node_modules/expo-speech-recognition/src/ExpoSpeechRecognitionModule.types.ts
index 28f323f..3613f64 100644
--- a/node_modules/expo-speech-recognition/src/ExpoSpeechRecognitionModule.types.ts
+++ b/node_modules/expo-speech-recognition/src/ExpoSpeechRecognitionModule.types.ts
@@ -125,6 +125,15 @@ export type LanguageDetectionEvent = {
   confidence: number;
   /** The alternative locales for the same language, in BCP-47 format. e.g. ["en-US", "en-GB"] */
   topLocaleAlternatives: string[];
+  /** Android 14+: whether the recognizer actually switched models. */
+  languageSwitchResult?:
+    | "succeeded"
+    | "failed"
+    | "skipped-no-model-or-not-allowed"
+    | "not-attempted"
+    | "unavailable"
+    | "unknown";
+  languageSwitchResultCode?: number;
 };
 
 /**
